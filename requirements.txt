# Tested with these minimum versions (Colab, Python 3.10+)
torch>=2.2
transformers>=4.45
peft>=0.13
datasets>=3.0
accelerate>=1.0
bitsandbytes>=0.44   # only needed for --load-4bit (QLoRA)
sentencepiece>=0.1.99
